export enum OnDeleteOptions {
  CASCADE = 'CASCADE',
  SET_NULL = 'SET NULL',
  NO_ACTION = 'NO ACTION'
}

export enum AppThemes {
  SYSTEM = 'system',
  LIGHT = 'light',
  DARK = 'dark'
}

export enum NotificationTypes {
  LIKES_RECEIVED = 'likes received',
  FOLLOWING_COMPLETES = 'following completes'
}

export enum SocialLoginPlatforms {
  KAKAO = 'kakao'
}

// TODO Update goal colors
export enum GoalColors {
  GRAY = 'gray'
}

export enum GoalPublicOptions {
  FULL_PUBLIC = 'full public',
  SOME_PUBLIC = 'some public',
  PRIVATE = 'private'
}
